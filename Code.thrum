let map = (arr: arr<num>, transform_fn) -> {
  let i = 0;
  while i < 3 {
    arr[i] = transform_fn(arr[i]);
    i += 1;
  }
  arr
}

let numbers = [10, 20, 30];

let tripled = map(numbers, (x) -> x * 3);

print("Tripled: {tripled}"); // Expected: [30, 60, 90]

tripled